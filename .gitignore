__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
# Artefacts generes (on garde output/.gitkeep)
output/*
!output/.gitkeep
.venv/
venv/
# Artefacts de build PyInstaller (voir opsforge.spec) et de l'installateur
# Inno Setup (voir opsforge-installer.iss) : regeneres a chaque build,
# jamais a committer (gros binaires).
build/
dist/
installer/
